# --------------------------------------------------------------------------
# This command restarts the Windows system and enters the firmware (BIOS/UEFI) setup utility after a delay of 1 second.
# --------------------------------------------------------------------------

shutdown.exe /r /fw /t 1